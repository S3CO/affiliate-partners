# AWS Deployment Environment Variables
# UYARI: Bu dosyayı production'da mutlaka güncelleyin!

# Backend API URL (AWS Lightsail IP'nizi buraya yazın)
API_URL=http://YOUR_LIGHTSAIL_IP:3008

# JWT Secret (Güvenli bir key oluşturun!)
# Örnek güvenli key oluşturmak için: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_MIN_32_CHARACTERS

# Port ayarları
FRONTEND_PORT=80
BACKEND_PORT=3008
